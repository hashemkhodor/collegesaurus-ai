# Runtime dependencies for the FastAPI chatbot service (chatbot/).
# Kept out of the root requirements.txt on purpose: Streamlit Community Cloud
# keeps resolving the root file for the legacy app until cutover.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
google-genai>=2.25,<3
numpy>=2,<3
httpx>=0.27,<1
python-dotenv>=1.0
