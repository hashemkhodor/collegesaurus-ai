# Lint config for the chatbot/ service and its tests. The legacy Streamlit
# modules at the repo root are left as they are until cutover.
target-version = "py311"
line-length = 100
include = ["chatbot/**/*.py", "tests/**/*.py"]

[lint]
select = ["E", "F", "W", "I", "B", "UP"]
